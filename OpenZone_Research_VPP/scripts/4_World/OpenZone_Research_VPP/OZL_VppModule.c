// Серверна половина склейки «дослідження у VPP».
//
// Порожня навмисно: усі реєстрації -- служба, розділ адмінки, файли конфігів
// у редакторі ядра -- живуть у базовому моді, бо потрібні серверу й без VPP.
// Цей pbo існує заради жорсткої залежності від вкладки OpenZone у VPP: панель
// RESEARCH знає про два моди одразу, тож живе окремо від обох.

[CF_RegisterModule(OZL_VppModule)]
class OZL_VppModule : CF_ModuleWorld
{
    override void OnInit()
    {
        super.OnInit();
    }
}
